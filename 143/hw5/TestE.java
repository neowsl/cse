// Short program to test whether the expressions generated using the file
// sentence2.txt are legal.  They can often be very long.  You should include
// the expression as indicated below and you should be able to compile the
// program.  It might throw an exception when run, but it should compile.

import static java.lang.Math.*;

public class TestE {
	public static void main(String[] args) {
		double x = 3;
		double y = 4;
		double z = 1
				* -pow(-tan(sin((92) - 0 * x) / 42
						* -min(0, -cos(1
								+ -max(pow(-(92 - ((y / 0) / x)),
										((42 % sin(1) - (((x % 42 / min(42, x) - 1 - x)) + x)) * y % 42 - y - -1) * 92)
										+ x / 1,
										abs(pow(y - y * -(42), sqrt((0))) * 42) - 1 / y / 42 % 1
												+ x % -max(92 + abs(92 - 92), min(92 * 92,
														42 - x + max(-(-92 + - -0 * 1 / 92 * sin(0 + 42)) - 0, tan(y)))
														- (max(42, max(
																max(0 / (pow(1 - 42, -sqrt(x)) / 92) / 1 * 92 + x,
																		y / sin((92 / -x))),
																abs(max(92, cos(92 * 42) * -max(x,
																		0 * 42 + (1 - 1 / 0 % abs(42) * -cos(1)) / 92
																				% tan(((92 + -1 % 92 * x)))))
																		* x))
																/ ((-1))))))
										* 92 * y
								+ pow(y, abs(y) + 0 % 0 / (min(x * -1 - 42 * x % 1 % 92, pow(1, 0)))))))
						* 92 * min(pow(42 % x, y), 0 / 42 / (92)), 1 % 0 - -92)
				* 42 / 92 + x;
	}
}
